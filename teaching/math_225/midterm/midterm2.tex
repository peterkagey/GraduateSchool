\documentclass[12pt]{exam}
\include{header}
\begin{document}
\include{first_page}
%
\begin{questions}
% \pointformat{}
\pagebreak

\question[5] {
  Let $V$ be an inner product space, and let $u_1, u_2$ be fixed (nonzero) vectors in $V$. Define $T: V\rightarrow \mathbb{R}$ by
  $$T(v)=(\langle u_1, v\rangle, \langle u_2, v\rangle).$$
  Use the properties of the inner product to show that $T$ is a linear transformation.
}
\pagebreak

\question[9] {
 Consider the transformation $T: P_2(\mathbb{R})\rightarrow P_1(\mathbb{R})$ defined by
 $$T(ax^2+bx+c)=(a+b)x+(a-c),$$
 where $a, b$ and $c$ are arbitrary real numbers.
   \begin{parts}
 	\part (3 points) Show that $T$ is a linear transformation.
 	\part (6 points) Determine $ker(T), Im(T)$, and their dimensions.
 \end{parts}
}
\pagebreak

\question[7] {
 Determine an orthogonal basis for the subspace of $C^0[-1, 1]$ spanned by $f_1(x)=1, f_2(x)=x^3, f_3(x)=x^4$.
}
\pagebreak

\question[8] {
Let $A=\left[{\begin{array}{ccc} 1 & 3 & -1\\ 2 & 7 & 9\\ 1 & 5 & 21\end{array}}\right]$, $b=\left[{\begin{array}{c} 4\\ 9\\ 6\end{array}}\right]$. Find all the solution of the system $Ax=b$.

}
\pagebreak
\question[6] {
  Let $V=C[0,1]$ and for $f$ and $g$ in $V$, consider the mapping
  $$\langle f, g\rangle=\int_{-1}^0 x^3 f(x)g(x)dx.$$
  Does this defines a valid inner product on $V$? Show why or why not.
}
\vspace{10cm}
\question[5] {
	Define $T: P_2(\mathbb{R})\rightarrow P_1(\mathbb{R})$ by $T(ax^2+bx+c)=c$. Determine whether $T$ is 1-1, onto, or neither. Find $T^{-1}$ or explain why it does not exist.
}
\end{questions}

\end{document}
